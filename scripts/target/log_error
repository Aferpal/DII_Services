#!/bin/bash

# $1 caller name to be put in brackets

# $2 log info

if [ $# -ne 2 ]; then
	log_error LOG_ERROR "Error trying to log an error, use two params"
	exit -1;
fi


echo -e "\033[1;31m[ $1 ] Error: \033[0m$2" >&2;
