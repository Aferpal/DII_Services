#!/bin/bash

# $1 caller name to be put in brackets

# $2 log info

if [ $# -ne 2 ]; then
	log_error LOG_INFO "Error trying to log a message, use two params"
	exit -1;
fi


echo -e "\033[1m[ $1 ]\033[0m $2";
